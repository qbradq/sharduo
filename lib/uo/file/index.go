package file

// IndexEntry represents one entry in an index file.
type IndexEntry struct {
}

// Index represents a MUL file index. These files typically have the same name
// as the MUL file they apply to with the .idx extension.
type Index struct {
}
